import { CoursesDirective } from './courses.directive';

describe('CoursesDirective', () => {
  it('should create an instance', () => {
    const directive = new CoursesDirective();
    expect(directive).toBeTruthy();
  });
});
